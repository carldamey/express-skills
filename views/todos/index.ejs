<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expresss To-Do</title>
</head>
<body>
    <h1>To-Do</h1>
    <ul>
        <% todos.forEach(todo => { %>
            <li>
                <%= todo.todo %>
                -
                <%= todo.done ? "done" : "not done" %>
            </li>
        <% }) %>
    </ul>
</body>
</html>